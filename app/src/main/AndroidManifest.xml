<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gyrodrive">

    <!-- Overlay flutuante sobre outros apps -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!-- Vibração para feedback tátil -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- Giroscópio (declarado mas não precisa de permissão runtime) -->
    <uses-feature android:name="android.hardware.sensor.gyroscope" android:required="true" />

    <!-- Foreground service para manter overlay vivo -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <application
        android:allowBackup="false"
        android:label="GyroDrive"
        android:icon="@drawable/ic_launcher"
        android:theme="@style/Theme.GyroDrive"
        android:supportsRtl="true">

        <!-- Activity principal: setup e permissões -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="landscape"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Serviço de overlay HUD -->
        <service
            android:name=".OverlayService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="game_controller_overlay" />
        </service>

        <!-- ★ AccessibilityService: injeta toques reais no xCloud ★ -->
        <service
            android:name=".GyroDriveAccessibilityService"
            android:exported="true"
            android:label="GyroDrive Controller"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

    </application>
</manifest>
